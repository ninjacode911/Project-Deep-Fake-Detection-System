{
    "default_upload_directory": "/home/ninja/Videos",
    "recent_files": [],
    "window_state": {
        "geometry": null,
        "maximized": false
    },
    "preferences": {
        "theme": "dark",
        "language": "en",
        "auto_analyze": true
    },

    "$schema": {
        "type": "object",
        "properties": {
          "upload_directory": {"type": "string"},
          "recent_files": {
            "type": "array",
            "items": {"type": "string"},
            "maxItems": 10
          }
        },
        "required": ["upload_directory", "recent_files"]
      }
}